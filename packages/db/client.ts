// Export runtime values (enums, namespaces)
export { Prisma, WorkspaceRole } from "@prisma/client";

// Export types only
export type {
  Workspace,
  User,
  WorkspaceInvite,
  VerificationToken,
} from "@prisma/client";
